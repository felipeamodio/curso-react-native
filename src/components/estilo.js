import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    txt: {
        marginTop: 10,
        fontSize: 20,
    },
    minmax: {
        fontSize: 30,
        textAlign: "center",
        padding: 60,
    },
    random: {
        fontSize: 20,
        marginTop: 10,
        fontWeight: "bold",
    },
    principal: {
        fontSize: 30,
    }
})